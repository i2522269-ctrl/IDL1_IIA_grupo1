streamlit
pandas
plotly
psycopg2-binary
numpy
scikit-learn
python-dotenv
prophet
# En la nube no hay GPU, asi que mejor la version CPU que pesa menos.
# Fijadas igual que las locales para que pip no se vaya de mambo.
tensorflow-cpu==2.21.0
torch==2.13.0
